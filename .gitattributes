# Ensure shell scripts always use LF line endings (even on Windows)
*.sh text eol=lf

# Ensure Docker-related files use LF
Dockerfile* text eol=lf
docker-compose*.yml text eol=lf

# Python files
*.py text eol=lf

# Default behavior for other text files
* text=auto
